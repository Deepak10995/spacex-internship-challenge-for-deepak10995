import React from 'react';
import DashboardBody from '../DashboardBody/index';
import Logo from '../Logo/Logo';

const Dashboard = (props) => {
  return (
    <div>
      <Logo />
      <DashboardBody />
    </div>
  );
};

export default Dashboard;
